# Data Agent 项目 PRD（生产级）与实施计划

## 1. 文档目的
本文件用于承接“继续下一步”的落地动作，将上一轮方案固化为可执行的产品需求文档（PRD）与实施计划，供产品、数据、工程、测试、运维共同执行。

---

## 2. 项目范围

### 2.1 项目目标
- 构建生产级 Data Agent，采用 **NL2LF2SQL** 架构。
- 实现“同指标同口径同结果”，并支持可追溯审计。
- 首期在单一高价值业务域（建议销售域）上线可用。

### 2.2 非目标
- 不做端到端 NL2SQL 主链路。
- 不做一次性全公司全域覆盖。
- 不以 Prompt 微调替代语义治理与确定性推理。

---

## 3. 用户与场景

### 3.1 目标用户
- 业务分析师
- 经营管理者
- 数据运营人员

### 3.2 核心场景
1. 自然语言问数（含同比、环比、占比、TopN后计算）
2. 自动解释（可读口径、时间窗、对象范围）
3. 一键报告（可追问）
4. 指标异动告警与归因初稿

---

## 4. 业务流程（主链路）

1. 用户输入自然语言问题。
2. NL 标准化服务进行上下文补全、省略恢复、指代消解。
3. Alisa 进行确定性推理，输出唯一 LogicForm。
4. SemanticDB 完成口径绑定、关系路径确定、权限校验。
5. 编译器将 LogicForm 翻译为目标数据库 SQL。
6. 执行层查询并返回结果。
7. 解释层返回“数值 + 口径说明 + 可追溯链路”。

---

## 5. 功能需求（FRD）

### FR-01 NL 标准化
**输入**：用户问题、会话上下文、用户身份。

**输出**：标准化 QueryObject（实体、指标、时间、过滤条件、歧义标签）。

**约束**：禁止直接生成 SQL、禁止做指标口径裁决。

### FR-02 确定性 LogicForm 生成
**输入**：QueryObject + 语义元数据。

**输出**：唯一 LogicForm（可序列化 JSON）。

**约束**：同输入同输出；支持未知词隔离支路，不污染主链路。

### FR-03 LogicForm 到 SQL 编译
**输入**：LogicForm、数据库方言、权限上下文。

**输出**：可执行 SQL（带 trace_id）。

**约束**：必须支持 ClickHouse/MySQL/PostgreSQL 三类方言扩展。

### FR-04 语义层（SemanticDB）
- 指标口径管理（版本化）
- 对象/层级/生命周期管理
- 时间语义统一（自然月、财务月、同比窗口）
- 权限治理（行级/列级/对象级）

### FR-05 解释与追溯
- 输出结果附“口径说明”
- 支持追溯 `NL标准化 -> LogicForm -> SQL -> 执行摘要`
- 提供审计查询 API

### FR-06 异常处理与降级
- 语义不完整时给出澄清问题
- 未知词进入隔离支路（向量召回/二次确认）
- 执行失败给出可操作错误信息（非技术用户可读）

---

## 6. 非功能需求（NFR）

- **性能**：100 表规模下 LogicForm 生成 P95 ≤ 500ms。
- **稳定性**：同输入 100 次输出一致率 100%。
- **可用性**：核心问答 API 月度可用性 ≥ 99.9%。
- **可扩展性**：新增指标不改代码（配置生效）。
- **可观测性**：分层指标（NL/LF/SQL）齐全。
- **安全性**：最小权限、全链路审计、敏感字段脱敏。

---

## 7. 数据与语义建模要求

### 7.1 数据层策略
- 保留 ADS 对现有 BI 的支持。
- 强化 DWD 作为 AI 主要计算来源。
- 抽离指标配置层，消除硬编码口径。

### 7.2 语义实体（最小可用模型）
- `metric_def`：指标定义、聚合方式、口径版本。
- `entity_def`：业务对象、生命周期规则。
- `dimension_def`：维度字典、层级关系。
- `relation_graph`：对象关系图与合法 JOIN 路径。
- `time_semantics`：时间语义映射与窗口规则。
- `policy_def`：权限规则与脱敏策略。

---

## 8. 验收标准

### 8.1 硬性验收
1. 100 表环境 LogicForm P95 ≤ 500ms。
2. 同输入 100 次一致率 100%。
3. 支持嵌套查询、临时分组、跨对象筛选、TopN后计算。
4. 同指标多问法口径一致。
5. 可追溯到完整执行链路。

### 8.2 四问检验法
1. 同义改写问法结果一致。
2. 改口径后问答/报告自动继承。
3. 异常可追溯到对象、事件、时间窗、计算路径。
4. 不同角色权限边界严格生效。

---

## 9. 实施计划（24 周）

### Phase 0（W1-W2）：范围冻结与基线
- 选定试点业务域
- 建立基准题集（100~300问）
- 定义验收门槛与监控口径

### Phase 1（W3-W6）：语义底座 V1
- 指标配置中心上线
- 对象/维度/时间语义入库
- 权限策略模型完成

### Phase 2（W7-W10）：NL 与 Alisa 主链路
- NL 标准化服务上线
- 确定性 LogicForm 服务上线
- 未知词隔离机制接入

### Phase 3（W11-W14）：SQL 编译与执行
- 方言编译器接入（CH + MySQL，PG 预留）
- 审计日志与回放接口上线
- 端到端压测

### Phase 4（W15-W18）：业务应用化
- 问答解释卡片
- 报告生成与追问闭环
- 四问检验与灰度上线

### Phase 5（W19-W24）：告警与扩域
- 指标监控告警与归因初稿
- 复制到第二业务域
- 运维手册与组织机制固化

---

## 10. Jira 任务拆解模板（可直接导入）

### Epic A：语义治理
- A1 指标口径模型设计
- A2 口径版本管理与回滚
- A3 对象层级与生命周期建模

### Epic B：核心引擎
- B1 NL 标准化 API
- B2 LogicForm Schema 与校验器
- B3 Alisa 图推理规则引擎
- B4 未知词隔离与二次确认

### Epic C：执行与安全
- C1 SQL 方言编译器
- C2 权限校验中间件
- C3 审计回放 API

### Epic D：产品化
- D1 问答解释 UI
- D2 报告生成器
- D3 告警中心接入

### Epic E：质量保障
- E1 一致性回归框架
- E2 压测与容量评估
- E3 四问检验自动化

---

## 11. 风险与缓解

1. **口径争议反复**：设立口径评审委员会 + 生效窗口制度。
2. **DWD 质量不足**：先治理 Top 指标相关数据链路。
3. **复杂查询误差**：建立难例集，迭代规则优先级。
4. **权限误配**：默认拒绝策略 + 越权扫描。
5. **跨库差异**：方言单测矩阵覆盖关键函数。

---

## 12. 下一步立即执行清单（两周内）

1. 冻结试点业务域与问题集。
2. 完成 LogicForm Schema V1。
3. 确定指标配置表结构与迁移方案。
4. 开通链路日志字段（trace_id/request_id/user_id）。
5. 启动 Phase 0 评审会并形成行动项。

---

## 13. 技术路线核准结论（是否可进入实施）

### 13.1 核准结论
**结论：技术路线正确，可以进入实施阶段。**

本项目采用的 **NL2LF2SQL（语言理解与结构化执行分层）** 路线，能够实质性解决“直接 NL2SQL”在生产环境中的三类核心问题：

1. **稳定性问题**：通过 Alisa 的确定性推理将结果空间收敛，避免端到端概率生成带来的抖动。
2. **治理问题**：通过 SemanticDB 显式承载指标口径、时间语义、关系路径和权限策略，实现问题定位与可运维。
3. **扩展问题**：将语言理解、语义推理、SQL执行解耦，支持业务复杂度增长下的渐进扩展。

### 13.2 能否真正解决当前痛点（逐项核验）

| 目标痛点 | 路线是否覆盖 | 核验结论 |
| --- | --- | --- |
| 同问法不稳定、结果漂移 | Alisa 确定性推理 + 一致性回归 | 可解决（前提：规则与语义元数据完备） |
| 指标口径不统一 | SemanticDB 指标配置与版本治理 | 可解决（前提：口径评审机制落地） |
| 结果不可解释不可追溯 | 全链路 trace：NL→LF→SQL→执行摘要 | 可解决 |
| 权限越权风险 | 语义层策略 + 执行前强校验 | 可解决（前提：默认拒绝策略） |
| 复杂查询难覆盖 | LF 层表达能力 + 方言编译器 | 可解决（前提：难例库持续扩展） |

### 13.3 进入实施阶段的前置门槛（Go/No-Go）

满足以下条件即 **Go**，否则 **No-Go**：

1. **试点域冻结**：明确首个业务域、Top 指标清单、核心题集（>=100问）。
2. **语义底座可用**：`metric_def / entity_def / relation_graph / policy_def` 最小闭环可用。
3. **验收口径签署**：性能、一致性、权限、追溯四类验收指标已被业务与技术共同签字。
4. **组织机制到位**：口径评审委员会与变更流程（版本、生效窗、回滚）生效。

### 13.4 实施阶段建议（立即执行）

- 按本文件第 9 章计划启动 **Phase 0（W1-W2）**。  
- 以“同输入100次一致率100%”和“口径一致性”作为第一优先验收门槛。  
- 每周进行一次“难例复盘会”，将线上歧义问题沉淀为规则/语义配置资产。

**最终判定**：在满足 13.3 前置门槛后，本项目应进入实施阶段。
